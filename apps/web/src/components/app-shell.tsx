'use client';
import { useState } from 'react';
import { AppSidebar } from '@/components/app-sidebar';
import { ThemeToggle } from '@/components/theme-toggle';
import { Input } from '@/components/ui/input';
import { Button } from '@/components/ui/button';
import { notifyToastPreview } from '@/lib/toast-feedback';
export function AppHeader() { const [menuOpen,setMenuOpen]=useState(false); return <header className="relative flex h-16 items-center justify-between gap-3 border-b border-slate-200 bg-white px-6 dark:border-slate-800 dark:bg-slate-900"><div className="text-sm text-slate-500">Workspace / Dashboard</div><Input aria-label="Search" className="hidden w-64 md:block" placeholder="Search tasks, projects..."/><div className="flex items-center gap-2"><ThemeToggle/><Button aria-label="Notifications" variant="ghost" size="sm" onClick={notifyToastPreview}>Notifications</Button><Button aria-label="Open account menu" variant="ghost" size="sm" className="rounded-full bg-indigo-100 font-bold text-indigo-700" onClick={()=>setMenuOpen(value=>!value)}>K</Button>{menuOpen&&<div role="menu" className="absolute right-6 top-14 z-30 w-40 rounded-xl border border-slate-200 bg-white p-1 shadow-lg dark:border-slate-700 dark:bg-slate-900"><button role="menuitem" className="w-full rounded-lg px-3 py-2 text-left text-sm hover:bg-slate-100 dark:hover:bg-slate-800">Profile</button><button role="menuitem" className="w-full rounded-lg px-3 py-2 text-left text-sm hover:bg-slate-100 dark:hover:bg-slate-800">Workspace settings</button></div>}</div></header> }
export function AppShell({children}:{children:React.ReactNode}) { return <div className="flex min-h-screen"><AppSidebar/><main className="min-w-0 flex-1"><AppHeader/>{children}</main></div> }