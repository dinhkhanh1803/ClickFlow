import { AppShell } from '@/components/app-shell';
export function PlaceholderPage({title, description}:{title:string;description:string}) { return <AppShell><section className="p-6"><p className="text-sm text-slate-500">ClickFlow / {title}</p><h1 className="mt-2 text-3xl font-bold">{title}</h1><div className="mt-6 rounded-xl border border-dashed border-slate-300 bg-white p-8"><h2 className="font-semibold">{description}</h2><p className="mt-2 text-sm text-slate-500">This Phase 1 screen uses mock state only. Feature implementation begins in Phase 2.</p></div></section></AppShell> }
