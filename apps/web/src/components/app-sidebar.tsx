'use client';
import Link from 'next/link';
import { useState } from 'react';
import { Button } from '@/components/ui/button';
import { Sheet, SheetTitle } from '@/components/ui/sheet';
import { useUiStore } from '@/stores/ui-store';
const items=[['Dashboard','/dashboard'],['My Tasks','/my-tasks'],['Calendar','/calendar'],['Time Tracking','/time-tracking'],['Projects','/projects'],['Templates','/templates'],['Reports','/reports'],['Archive','/archive'],['Settings','/settings']];
export function AppSidebar(){const [open,setOpen]=useState(false); const openCreateTask=useUiStore(state=>state.openCreateTask); const navigation=<nav className="space-y-1">{items.map(([label,href])=><Link key={label} href={href} onClick={()=>setOpen(false)} className="block rounded-lg px-3 py-2 text-sm text-slate-600 hover:bg-indigo-50 hover:text-indigo-700 dark:text-slate-300 dark:hover:bg-slate-800">{label}</Link>)}</nav>; return <><Button aria-label="Open navigation" className="fixed left-3 top-3 z-40 md:hidden" onClick={()=>setOpen(true)}>Menu</Button><aside className="sticky top-0 hidden h-screen w-64 shrink-0 self-start flex-col border-r border-slate-200 bg-white p-4 dark:border-slate-700 dark:bg-slate-900 md:flex" aria-label="Primary navigation"><Link href="/dashboard" className="mb-8 text-xl font-bold text-indigo-600">ClickFlow</Link>{navigation}<Button className="mt-auto" onClick={openCreateTask}>Create New</Button></aside><Sheet open={open} onOpenChange={setOpen}><Button aria-label="Close navigation" variant="ghost" onClick={()=>setOpen(false)}>Close</Button><SheetTitle className="sr-only">Mobile navigation</SheetTitle><Link href="/dashboard" className="my-6 block text-xl font-bold text-indigo-600">ClickFlow</Link>{navigation}<Button className="mt-6 w-full" onClick={()=>{openCreateTask();setOpen(false)}}>Create New</Button></Sheet></>}