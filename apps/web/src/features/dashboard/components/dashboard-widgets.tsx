import type { Deadline, ProjectSummary } from '@clickflow/contracts';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function DashboardWidgets({ weeklyHours, deadlines, projectHealth }: { weeklyHours: number[]; deadlines: Deadline[]; projectHealth: ProjectSummary[] }) {
  const maxHours = Math.max(...weeklyHours);
  return <div className="mt-6 grid gap-6 xl:grid-cols-3"><Card className="xl:col-span-2"><CardHeader><CardTitle>Project Health</CardTitle></CardHeader><CardContent className="grid gap-5 md:grid-cols-2">{projectHealth.map(project => <div key={project.id}><div className="flex justify-between gap-3 text-sm"><span className="font-medium">{project.name}</span><span className={project.health === 'on-track' ? 'text-emerald-600' : 'text-rose-600'}>{project.health === 'on-track' ? 'On track' : 'At risk'}</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-100 dark:bg-slate-800"><div className={project.health === 'on-track' ? 'h-full bg-indigo-500' : 'h-full bg-rose-500'} style={{ width: `${project.progress}%` }} /></div><p className="mt-1 text-right text-xs text-slate-500">{project.progress}% complete</p></div>)}</CardContent></Card><Card><CardHeader><CardTitle>Weekly Hours</CardTitle></CardHeader><CardContent><div className="flex h-36 items-end justify-between gap-2" aria-label="Weekly hours chart">{weeklyHours.map((hours, index) => <div key={index} className="flex flex-1 flex-col items-center gap-2"><div className="w-full rounded-t bg-indigo-500" style={{ height: `${(hours / maxHours) * 100}%` }} title={`${hours} hours`} /><span className="text-xs text-slate-500">{['M','T','W','T','F','S','S'][index]}</span></div>)}</div></CardContent></Card><Card className="xl:col-span-3"><CardHeader><CardTitle>Upcoming Deadlines</CardTitle></CardHeader><CardContent className="grid gap-4 md:grid-cols-3">{deadlines.map(item => <div key={item.id} className="border-l-2 border-indigo-400 pl-3"><p className="text-sm font-medium">{item.title}</p><p className={item.tone === 'urgent' ? 'mt-1 text-sm text-rose-600' : 'mt-1 text-sm text-slate-500'}>{item.due}</p></div>)}</CardContent></Card></div>;
}