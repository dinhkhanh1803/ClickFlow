'use client';

import { useState } from 'react';
import { Bookmark, FileText, Folder, FolderKanban, ListChecks, Plus, RefreshCw, Settings2, Sparkles } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Dialog, DialogTitle } from '@/components/ui/dialog';
import { useWorkspace } from '../model/workspace-store';
import { SpaceTabContent, type SpaceView } from './space-tab-content';

export function SpaceOverview() {
  const { space, selectProject, createProject } = useWorkspace();
  const [creating, setCreating] = useState(false);
  const [bookmarked, setBookmarked] = useState(false);
  const [activeView, setActiveView] = useState<SpaceView>('Overview');
  const [name, setName] = useState('');
  const [description, setDescription] = useState('');
  const projects = space.projects.filter((project) => !project.archived);
  const recentTasks = projects.flatMap((project) => project.tasks.map((task) => ({ project, task }))).slice(0, 3);
  const openFirstProject = () => { if (projects[0]) selectProject(projects[0].id); };
  const submit = (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!name.trim()) return; createProject({ name: name.trim(), description: description.trim() || 'New project in this Space.' }); setName(''); setDescription(''); setCreating(false); };

  return <main className="min-h-[calc(100vh-4rem)] bg-white text-slate-900 dark:bg-slate-950 dark:text-slate-100"><header className="border-b border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-950"><div className="flex min-h-14 flex-wrap items-center justify-between gap-3 px-5"><div className="flex items-center gap-2 text-sm"><span className="grid h-4 w-4 place-items-center rounded bg-indigo-500 text-[9px] font-bold text-white">T</span><span className="font-semibold">{space.name}</span><button aria-label="Space menu" className="text-slate-400 hover:text-slate-700">⌄</button><button aria-label="Favorite Space" className="text-slate-400 hover:text-amber-500">☆</button></div><div className="flex items-center gap-2 text-sm text-slate-600 dark:text-slate-300"><button className="hover:text-indigo-600">Agents</button><button className="hover:text-indigo-600">Automate</button><button className="inline-flex items-center gap-1 hover:text-indigo-600"><Sparkles size={15} />ClickFlow AI</button><Button size="sm" onClick={() => setCreating(true)}><Plus size={15} />Project</Button></div></div><div role="tablist" aria-label="Space views" className="flex items-center gap-1 overflow-x-auto border-t border-slate-100 px-4 dark:border-slate-900">{(['Overview', 'Board', 'List', 'Calendar', 'Gantt', 'Table'] as SpaceView[]).map((view) => <button key={view} role="tab" aria-selected={activeView === view} onClick={() => setActiveView(view)} className={`flex shrink-0 items-center gap-1 border-b-2 px-2.5 py-3 text-sm ${activeView === view ? 'border-slate-900 font-semibold dark:border-white' : 'border-transparent text-slate-500 hover:text-indigo-600'}`}>{view === 'Board' ? <FolderKanban size={15} className="text-blue-500" /> : view === 'List' ? <ListChecks size={15} className="text-slate-500" /> : null}{view}</button>)}<button onClick={openFirstProject} className="flex shrink-0 items-center gap-1 px-3 py-3 text-sm text-slate-500 hover:text-indigo-600"><Plus size={15} />View</button></div></header><div className="flex items-center justify-center border-b border-indigo-100 bg-indigo-50 px-5 py-3 text-center text-sm text-indigo-950 dark:border-indigo-900/60 dark:bg-indigo-950/30 dark:text-indigo-100">Customize this Space around the way your team works. <button className="ml-1 font-semibold underline">Get started</button></div><div className="flex items-center justify-end gap-3 border-b border-slate-100 px-5 py-3 text-xs text-slate-500 dark:border-slate-900"><span className="inline-flex items-center gap-1 text-emerald-600"><RefreshCw size={13} />All changes saved</span><button className="rounded-full border border-indigo-200 bg-indigo-50 px-2 py-1 text-indigo-700 dark:border-indigo-900 dark:bg-indigo-950 dark:text-indigo-200">Auto refresh: On</button><button aria-label="Overview settings" className="rounded border border-slate-200 p-1.5 hover:text-indigo-600 dark:border-slate-700"><Settings2 size={15} /></button></div>{activeView === 'Overview' ? <><section className="grid gap-4 p-4 lg:grid-cols-[1fr_1fr_1fr]"><article className="min-h-72 rounded-xl border border-slate-200 bg-white p-4 dark:border-slate-800 dark:bg-slate-950"><h2 className="text-sm font-semibold">Recent</h2><div className="mt-4 space-y-3">{recentTasks.length ? recentTasks.map(({ project, task }) => <button onClick={() => selectProject(project.id)} key={task.id} className="flex w-full items-center gap-3 text-left text-sm hover:text-indigo-600"><ListChecks size={16} className="shrink-0 text-slate-500" /><span className="truncate">{task.title} <span className="text-slate-400">· in {project.name}</span></span></button>) : <p className="text-sm text-slate-500">No recent work yet.</p>}</div></article><article className="min-h-72 rounded-xl border border-slate-200 bg-white p-4 dark:border-slate-800 dark:bg-slate-950"><h2 className="text-sm font-semibold">Docs</h2><div className="mt-4 space-y-3">{projects.slice(0, 3).map((project) => <button key={project.id} onClick={() => selectProject(project.id)} className="flex w-full items-center gap-3 text-left text-sm hover:text-indigo-600"><FileText size={16} className="text-slate-500" /><span>{project.name} brief <span className="text-slate-400">· in {project.name}</span></span></button>)}</div></article><article className="flex min-h-72 flex-col rounded-xl border border-slate-200 bg-white p-4 dark:border-slate-800 dark:bg-slate-950"><h2 className="text-sm font-semibold">Bookmarks</h2><div className="flex flex-1 flex-col items-center justify-center text-center"><Bookmark size={44} className="text-slate-300" /><p className="mt-4 max-w-xs text-sm text-slate-500">Save a Space, project, task, or useful link for quick access.</p>{bookmarked && <button onClick={openFirstProject} className="mt-3 text-sm font-semibold text-indigo-600">{projects[0]?.name}</button>}<Button size="sm" className="mt-4" onClick={() => setBookmarked((value) => !value)}>{bookmarked ? 'Bookmark saved' : 'Add Bookmark'}</Button></div></article></section><section className="mx-4 mb-4 min-h-80 rounded-xl border border-slate-200 bg-white p-4 dark:border-slate-800 dark:bg-slate-950"><div className="flex items-center justify-between"><h2 className="text-sm font-semibold">Folders</h2><Button size="sm" variant="ghost" onClick={() => setCreating(true)}><Plus size={15} />New project</Button></div><div className="mt-4 flex flex-wrap gap-3">{projects.map((project) => <button key={project.id} onClick={() => selectProject(project.id)} className="flex min-w-64 items-center gap-3 rounded-lg border border-slate-200 px-4 py-3 text-left text-sm font-semibold hover:border-indigo-300 hover:bg-indigo-50 dark:border-slate-700 dark:hover:bg-indigo-950/30"><Folder className="text-slate-500" size={20} />{project.name}<span className="ml-auto text-xs font-normal text-slate-400">{project.tasks.length} tasks</span></button>)}</div></section></> : <SpaceTabContent view={activeView as Exclude<SpaceView, 'Overview'>} projects={projects} onOpenProject={selectProject} />}<Dialog open={creating} onOpenChange={setCreating}><form onSubmit={submit} className="space-y-4"><DialogTitle>Create a project</DialogTitle><label className="block text-sm font-medium">Project name<input aria-label="Project name" value={name} onChange={(event) => setName(event.target.value)} className="mt-2 w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm outline-none focus:border-indigo-500 dark:border-slate-700 dark:bg-slate-950" autoFocus /></label><label className="block text-sm font-medium">Description<textarea aria-label="Project description" value={description} onChange={(event) => setDescription(event.target.value)} className="mt-2 min-h-24 w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm outline-none focus:border-indigo-500 dark:border-slate-700 dark:bg-slate-950" /></label><div className="flex justify-end gap-2"><Button type="button" variant="ghost" onClick={() => setCreating(false)}>Cancel</Button><Button type="submit">Create project</Button></div></form></Dialog></main>;
}