# Information Architecture

Sitemap: Login; Dashboard; My Tasks; Calendar; Time Tracking; Projects; Project Detail; Templates; Reports; Archive; Settings.

Navigation chính dẫn đến các route dự kiến `/dashboard`, `/my-tasks`, `/calendar`, `/time`, `/projects`, `/templates`, `/reports`, `/archive`, `/settings`. Project detail lồng sections, list/Kanban, task detail và activity. Route thực thi sẽ được quyết định ở Phase 1.
