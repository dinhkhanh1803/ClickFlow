# Logging and Observability

Log cấu trúc có timestamp, level, requestId, userId đã giảm thiểu và event; không log password/token/PII nhạy cảm. Theo dõi error rate, latency, database connection, job/upload failure và uptime. Alert gắn runbook; audit activity khác với application log và không sửa được.
